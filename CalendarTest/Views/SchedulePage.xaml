<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:xForms="clr-namespace:Syncfusion.SfSchedule.XForms;assembly=Syncfusion.SfSchedule.XForms"
             xmlns:viewModels="clr-namespace:CalendarTest.ViewModels;assembly=CalendarTest"
             mc:Ignorable="d"
             x:Class="CalendarTest.Views.SchedulePage">
    <ContentPage.Content>
        <xForms:SfSchedule FirstDayOfWeek="2" ScheduleView="DayView" DataSource="{Binding AppointmentCollection}" TimeInterval="30" DayViewSettings="{Binding DayViewSettings}" ShowCurrentTimeIndicator="True">
            <xForms:SfSchedule.SelectionStyle>
                <xForms:SelectionStyle 
                    BackgroundColor="Blue" 
                    BorderColor="Black"
                    BorderThickness="5"
                    BorderCornerRadius="5">
                </xForms:SelectionStyle>
            </xForms:SfSchedule.SelectionStyle>
        </xForms:SfSchedule>
    </ContentPage.Content>
    <ContentPage.BindingContext>
        <viewModels:MainPageViewModel />
    </ContentPage.BindingContext>
</ContentPage>